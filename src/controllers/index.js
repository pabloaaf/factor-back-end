const authController = require('./oauth.controller.js');
const videoController = require('./video.controller');
const courseController = require('./course.controller');
const userController = require('./user.controller');

module.exports = {
  authController,
  videoController,
  courseController,
  userController
};
